# Short persistence Oracle v3

Version 3 is frozen before its next validation pool. It keeps the v2
warmup-prefix rule and increases the signed CountSketch dimension from 64 to
256. This is still far smaller than a declared parameter carrier and retains
the same sign-flip null, ordered-state requirement, and exact-confirmation
gate.

The 64-dimensional and 256-dimensional Ministral observations are recorded as
engineering power diagnostics, not as held-out claims. Future validation must
use v3 from the start, with a pre-enumerated implementation pool and centered
controls retained in the denominator.

