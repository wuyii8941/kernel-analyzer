# Direct Persistence v4：简单说明

> 历史短程说明。当前科研主线、长程计数和训练等价性定义见
> `docs/current_mainline.md`；本页数据不能覆盖当前 301-row machine audit。

## 这轮实验在回答什么

我们不再只看“误差有多大”。我们把同一个实现和修复版放在相同的输入、参数和优化器状态下，观察两者产生的更新差异是否会连续地朝同一个方向累积。

这里的 `A` 是一个方向性分数：

- `A` 接近 1：差异像随机噪声，通常会互相抵消；
- `A` 明显大于 1：差异更像朝一个方向累积；
- 最终参数分开，不一定是算子本身造成的，也可能是两条轨迹分开后产生的反馈。

因此每个案例分开报告三种变化：

1. 算子本步直接造成的变化；
2. 两条轨迹分开后产生的反馈变化；
3. 最终实际参数变化。

## 已经得到的短程数据

统一的 cold-start AdamW（moment 从零开始，随后正常更新）回溯数据中：

| 案例 | 直接变化 A | 反馈变化 A | 实际变化 A | 当前解释 |
|---|---:|---:|---:|---|
| Liger fused CE | 1.720 | 3.494 | 3.489 | 直接作用存在，反馈后来更相干 |
| Phi lm_head dX | 1.029 | 5.075 | 1.711 | 直接作用很小但可测，优化器会改变它的表现 |
| Qwen lm_head dX | 0.957 | 1.191 | 1.508 | 这个 cold-start 32 步窗口内直接作用接近抵消 |

同一 Phi 案例的不同测量不能混用：

- `A=4.701` 是无状态 SGD 映射下的 32 步更新差异；
- `A=1.029` 是 cold-start AdamW 下的 32 步直接更新差异；
- `A=4.488` 是另一个四臂实验中最终 norm 参数的实际分离。

它们不是同一个数字，也不是一个范围。

## 长程结果优先于短程标签

当前长程协议先 warm up 128 步，再测 4096 步同状态直接更新。本页后续表格只保留 v4 当时的代表行，不能当作当前总计。最新机器审计为 301 行；43 行有 long-run bias evidence 与 paired loss split，但只有 4 行目前有显式 late-window confirmation。完整逐行结果见 `results/property/declared_persistent_4096/all_bias_case_audit.json`：

| 案例 | A4096 | 后半程 32 步窗口 | 当前长程判断 |
|---|---:|---:|---|
| Liger fused CE | 14.018 | 64/64 有方向 | 稳健直接方向；参数/loss gap 已观察 |
| Phi lm_head dX | 46.090 | 64/64 有方向 | 稳健长程方向 |
| Qwen lm_head dX | 6.488 | 64/64 有方向 | 稳健长程方向 |
| Llama lm_head dX | 5.881 | 超过自身随机基线 | 同一实现族的长程直接方向 |
| Ministral lm_head dX | 5.050 | 超过自身随机基线 | 同一实现族的长程直接方向 |
| Mamba in_proj | 0.935 | 未超过自身随机基线 | 直接方向不稳健；live 配对 loss 已完成并出现分叉 |
| Qwen saved-P | 1.195 | 28/64 有方向 | 直接方向不稳健，但 live loss 已长程分叉 |
| Qwen seq64/128 v_proj | 1.000 / 0.981 | 不超过自身随机基线 | 直接方向不稳健；seq64 live loss 已长程分叉 |
| Qwen3-VL SiLU | 3.100（反馈） | 长程反馈维持 | 反馈维持型长程案例；配对 loss gap 已记录 |
| layer-23 attention | — | — | 实现身份变化，拒绝判断 |
| Gemma4 RMSNorm | — | — | 兼容长程重放在第 294 步后未产出完整结果，单独未决，不判阴性 |
| DeepSeek layer-35 dV | — | — | 形成阶段未确认 |

因此，Qwen 的 `0.957` 只能说明旧 cold-start 32 步窗口内抵消，不能再写成“Qwen 被 AdamW 永久消掉”。4096 步仍不是 loss 收敛实验；配对 loss gap 只表示已经观察到分叉，不要求不同的最终收敛值。

## 反馈对照说明了什么

从结果盲的 12 个控制项中：

- 11 个的直接变化接近随机抵消，但后续反馈仍会让轨迹分开；
- 1 个同时含有直接作用和反馈作用。

所以“最终参数变了”不能直接证明“算子本身有持续偏差”。必须先看直接变化。

## 前瞻检查

新冻结的 Gemma 实现检查完成了 3 行：

- 0 行直接持续正例；
- 1 行是反馈型对照；
- 2 行没有可测的参数载体作用，因此标为“不适用”，没有硬改成负例。

因此这批数据不能计算前瞻 recall 或 AUROC，也不能证明通用安全。

## 误差标准对比

3 个新的 Gemma 行保存了输出、梯度和同状态更新的 candidate/repair 对，可以计算 ULP 和 `rtol/atol`。历史案例缺少原始 operand 和位级数据，因此完整冻结样本的 tolerance 对比仍保持 `ABSTAIN`，没有用相近文件补齐。

## 当前可以说什么

当前结果支持一个有明确范围的两级工具：

> 先用 16/32 步更新方向性排序检查优先级，再用 4096 步和后半程窗口给长程方向标签；最终轨迹还要把直接作用和反馈作用分开。

## 当前不能说什么

- 不能说已经得到所有算子都适用的安全判定器；
- 不能说优化器就是数值问题的根因；
- 不能把所有最终轨迹分离都归因给算子；
- 不能把 3 个 Gemma 控制项当成通用前瞻准确率；
- 不能把缺少原始数据的 tolerance 指标当成已完成。

所有缺少必要原始证据的项目都保留为 `ABSTAIN`，而不是填成通过或失败。

## 当前收口决定

当前文稿以已完成的 4096 步结果为准。若要进一步声称完整全参数训练在收敛后达到不同终点，仍需独立的全参数收敛实验。
