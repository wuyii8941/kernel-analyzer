# 主线整理记录：2026-09-05

> 历史整理记录，已被同日后续的训练数值分析主线取代。本页只说明当时做过什么，
> 不再定义当前研究中心或后续实验要求。

整理基于本地干净 main@c726911af41efc3d8cccb62dc4cdbf1d3f99341b。
这是文档、来源与接口语义核对，不是 GPU 复现实验或新方法冻结。

## 当时保持的研究中心

**数学推导解释 bias，真实 backward/optimizer 检查其进入更新，配对训练观察 loss
分叉，针对成因的修改验证解释。** 不要求 loss 持续变差、不同收敛终点或崩溃。
有推导不等于其非零条件在所有案例都成立；loss 分叉也不能倒推成因。

## 对审查建议的选择

- 采纳：实际执行位置与实现语言分开；参考不是默认真值。
- 采纳：自然训练共享进入被测计算前的状态，不强行要求两边内部 saved tensors 相同。
- 采纳：完整 Gram 与 CountSketch 分开；三个摘要最大值不提升为原空间理论上界。
- 采纳：固定集合与总体区间分开，工程阈值和结果揭示后的修正保留时间范围。
- 采纳：旧 T1–T4、参考数据预测器保留原用途，不把它们当成当前研究的唯一入口。
- 采纳：最新 10000 步 Liger 进入主线，去掉持续恶化和普遍不会崩溃的外推。
- 当时未采用：把论文中心改成统一分析与等价性检验。后续研究决定已经改变，当前
  以 [科研主线](current_mainline.md) 为准。

## 实际修正

1. 重整 README、PROJECT、当前主线、方法、主张及贡献边界；移除重复维护的旧计数。
2. 新增案例地图，原始结果直接链接，区分同名案例的不同模型、参数和训练状态。
3. 标明五例 profile v2 的 ZERO_AT_EVERY_INPUT_STATE；不混称连续 32 步零初始 moments。
4. 核查当前 Liger 0.7.0 安装源码的 CE/Triton 与 dW 常规累加混合路径。
   没有据此补造历史运行的执行身份。
5. 修正 Liger 崩溃标准说明，区分触发条件与进一步确认，注明单条 10000 步延续
   没有逐步同状态直接/反馈重放。
6. 旧文档加版本定位。历史推导、门槛、失败和未决仍可追溯，不重新定义旧实验标签。
7. 旧 API 增加用途说明；v1 三投影脚本运行时提醒不构成完整向量等价。未改数值算法。

## 保留、简化与未做事项

重复叙事和过期的“当前总数”从主要入口删去；旧案例文件仍被脚本或结果说明引用，
数学推导也有保留价值，因此不删除它们。原始 JSON/CSV、冻结协议和运行记录不改；
results/README.md 只更新导航。讲稿 talk_beyond_tolerance.md 不改。

没有新建替代分析 API，没有改旧结果的 PASS/FAIL，没有重跑训练；这不是宣称所有
案例已完成同协议数学到 loss 的闭环。若以后需要更强的原坐标能量保证，应使用
真实原坐标累加或有定量误差保证的方法，不能靠此次文字整理得到。

只读检查脚本 [check_research_docs.py](../scripts/check_research_docs.py) 检查当前入口
链接、引用数字和指定基线以来的数据/讲稿保护。它不证明实际运行身份或数学推导正确。

本轮检查：当前 12 个入口/说明文件的链接与关键数字核对通过；等价工具、系统与
预测接口的 38 项已有 CPU 测试通过；旧 v1 脚本帮助入口可运行。未重新采集实验。
