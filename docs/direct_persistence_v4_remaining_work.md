# v4 收口状态与可选后续

当前稿件所需的统计校正、optimizer 对照、直接作用/状态反馈分解和三行新实现检查已经完成。项目决定停止扩展实验并进入写作收口。

## 已完成

- 15 行统一 cold-start AdamW 回溯分析；
- 三组 Holm 多重比较；
- Liger、Phi、Qwen 的直接作用、状态反馈和实际参数变化分解；
- 4 个案例的同状态 optimizer 对照；
- Qwen 早期、中期、后期自然状态响应；
- 3 行新 Gemma 实现检查；
- 保存了原始数据的案例所能支持的方向性、ULP 和 `rtol/atol` 重算；
- 缺失数据全部保持 `ABSTAIN`，没有从相近案例补填。

## 当前有意保留的边界

1. `0543` 的旧运行包无法精确重放，保持未决候选。
2. 历史冻结池缺少完整位级输入，不能生成全池 tolerance 表。
3. 新实现池没有直接持续正例，不能计算前瞻 recall/AUROC。
4. 没有前瞻正例，catch-and-fix 为不适用。
5. 参数总范数、loss 投影和真实 loss 没有统一协议，完整严重度保持未决。
6. v4.1 已冻结但未运行；它不是当前结果的一部分。

## 是否必须继续

不必须。以上六项只限制更强的“跨未见实现泛化、完整 tolerance、安全分类和自动修复”主张，不影响当前有界结论：

> cold-start AdamW 下的短程直接持续性可以作为后续完整检查的分诊依据；最终参数分离必须拆分为算子直接作用和训练状态反馈。
