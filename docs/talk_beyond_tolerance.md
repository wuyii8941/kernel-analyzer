# 超越 Tolerance：怎样判断一个训练算子的数值误差会不会形成 bias，并影响 loss

# 第一部分：问题是什么

---

## Slide 1｜问题

数值测试通常比的是：两个结果差多少？差得小就通过。

但在训练里还要问另一件事：**这些差异的正负最终会互相抵消，还是会形成有方向的偏差？**

一个比较大但方向来回变的误差，可能只是多一点噪声。一个小得多但反复朝同一边的误差，可能被训练不断写入参数。

今天不是要否定 tolerance。它很适合抓明显的数值错误。我们要补的是它没回答的那一半：

> **正负误差经过真实训练路径后还能不能抵消？如果不能，这个 bias 会不会进一步造成 loss 分叉？**

所以后面的证据分成两层：

- **第一层（Bias formation）**：抵消是否被破坏
- **第二层（Bias consequence）**：在配对长程实验中，candidate 和 repair 是否出现 loss 分叉

【图 · Slide 1】建议画一个简洁的两层示意：上层"抵消被打破？"，下层"loss 分叉？"，中间箭头连接。与传统 tolerance 的"误差 < 阈值？"单层对比并排。

---

## Slide 2｜训练是一个有状态的循环

不熟悉训练的听众只需知道四步：

- **前向**：模型读输入，经过矩阵乘法、归一化、激活函数等算子，得到预测
- **算 loss**：预测和正确答案差多远
- **反向**：从 loss 倒着算，得到每个参数应该怎么调（梯度）
- **优化器**：结合当前梯度和历史状态，决定真正写入参数的更新量

一个局部差异要影响参数，必须穿过整条链：

```
算子误差 → backward → gradient → optimizer → parameter update → 下一步状态改变
```

两条轨迹一旦分开，下一步的状态也不同，后续还会产生反馈。

**训练不是一次函数调用，是一个有状态、带反馈的循环。**

【图 · Slide 2 · 重要】画一个训练循环的完整示意图：五个方框（forward → loss → backward → optimizer → parameter update）形成一个环，最后一步用箭头回指 forward 表示"进入下一步"。在"算子误差"处标一个红点，然后用虚线追踪这个红点穿过每一步，直到写入参数。**这张图贯穿全篇，后面每讲到链条的某一段都可以回指它。**

---

## Slide 3｜起点：FlashAttention 的教训

这个问题来自一个真实的训练崩溃。

FlashAttention 和标准 attention 在数学上完全等价，但改变了分块和累加顺序。在 BF16（一种 16 位浮点格式，精度低但速度快）下，某些累加模式让舍入误差**反复偏向同一侧**——不是有正有负，是单边的。

这个偏差经过反向传播，每一步给参数推一点点。多步之后，参数明显偏离，训练崩了。

**关键是修复方式**：只改掉触发单侧舍入的那个局部条件，训练就重新稳定了。说明找到的是原因，不只是相关。

这篇工作（Qiu & Yao, ICLR 2026）最重要的启示是：

> **误差小不代表安全，数学等价不代表训练行为等价。真正要问的是正负能否抵消。**

Golden et al. (2024) 也做过类似的 matched implementation 对比，追踪了 FlashAttention 的权重发散。但他们自己写明只测了单次前向、只测了 attention。**这两条限制正好是我们要补的**——我们要覆盖真实反向、覆盖优化器、覆盖一个训练步里的所有算子。

【图 · Slide 3】引入原论文中的图

---

# 第二部分：核心定义

---

## Slide 4｜Cancellation Preservation：对消只会在两类地方失败

理想情况下，+ε 和 −ε 出现得一样多，经过训练路径后对参数的影响互为相反。

**安全不要求误差为零。安全要求正负误差经过真实训练路径后还能互相抵消。**

我们把这个叫 **Cancellation Preservation**（抵消保持）。

对消失败可以归成两类根因：

**根因 1：正负误差事件或配对不平衡**

理想是 +ε 和 −ε 出现得一样多，碰到相似的训练状态。实际可能出现：

- 固定累加顺序让舍入偏向一侧
- 正负误差分别碰上不同的反向状态
- forward 保存值和 backward 使用值之间的合同差异

代表案例：**Liger**（BF16 分块累加造成单侧方向）、**Phi**（误差和反向状态的固定配对造成梯度方向）

**根因 2：正负响应不对称**

即使人为构造了严格相反的 +ε / −ε，经过反向或优化器后，参数更新也可能不再互为相反数。

代表案例：**saved-P** 和 **SiLU**（对严格相反的梯度残差重放，观察到优化器的非对称响应）

| 根因 | 工程上的表现 |
|---|---|
| 正负事件或配对不平衡 | 累加偏差、舍入事件分布、backward 配对、forward/backward 状态合同差异 |
| 正负响应不对称 | 非线性 backward、optimizer 的非奇对称响应 |

这个思路和优化理论里一个成熟的结论一致：Karimireddy et al. (ICML 2019) 证明了有偏的梯度压缩会让训练不收敛，无偏的不会。**低精度 kernel 可以看成一个隐式的有偏压缩算子**——我们检测的正是它引入的偏差，而不是方差。

---

## Slide 5｜两类根因怎样落在训练链上

回到 Slide 2 那张训练循环图。把两类根因标在链条上：

```
实现与数据 → 局部误差事件 (ε, state)
                    │
                    ├─ 根因 1 在这里：+ε / −ε 的事件数量或配对不公平
                    ↓
          backward / optimizer response
                    │
                    ├─ 根因 2 在这里：R(+ε) + R(−ε) ≠ 0
                    ↓
          parameter update difference
                    │
                    ├─ 低秩：只描述"偏差长什么样"
                    ↓
           后续训练反馈：放大、保留或压制
```

全篇回答三个层次分明的问题：

当我们发现一个训练产生Bias，我们需要回答的三个问题是

1. **Formation**：两类根因中的哪一类打破了抵消？
2. **短程证据**：16/32 步里能否测到方向，用于机制定位和实验排序
3. **长程复核与后果**：4096 步后半程 bias 本身是否仍有方向，并且配对实验中是否出现参数和 loss 分叉

这里有一个硬口径：**只有 bias-bearing 的直接作用或反馈作用本身在 4096 步后半程仍然通过自己的随机基线，才叫持久性 bias。**只有 loss 分叉、但没有持续 bias 组件的记录，只能作为后果对照；无法安全重放的记录保留为未决，不改写成阴性。

【图 · Slide 5】在 Slide 2 的训练链图上叠加标注：根因 1 的位置、根因 2 的位置、低秩和反馈各自的角色。用不同颜色区分"根因"和"后续过程"。

---

## Slide 6｜我们测了哪些模型

全量普查覆盖四个核心模型，每个检查了序列长度 64、128 和 256：

| 模型 | 架构特点 | 为什么选它 |
|---|---|---|
| Qwen3-1.7B | 标准 Transformer | attention 和 loss 路径记录最完整 |
| Phi-4-mini | 不同词表和矩阵形状 | 不同编译后代码路径 |
| DeepSeek-R1-Qwen3-8B | 更大的模型 | 不同 attention 和参数形状 |
| Mamba-130M | 非 attention 架构 | state-space 更新，验证发现不限于 Transformer |

四个模型完成了 **1,562 / 1,562** 个声明位置的全坐标首轮检查。

另外六个模型（Qwen3-VL、Gemma、OLMoE、Llama、Ministral）用于定向实验、held-out 或跨模型复现，不等同于全量普查。

> **1,562 是全量普查和首轮检查，不是 1,562 个完整训练实验。**只有差异非零、能到达参数、且有合法对照的记录才进入深测。

---

## Slide 7｜完整案例地图：14 个实现差异，回到两类根因

下面一张表列出所有主要案例。它不把"最后权重变了"一律算成算子偏差，而是说明方向最早在哪出现、对应哪类根因、当前结论是什么。

| # | 案例 | 根因 | 已支持的证据 | 当前边界 |
|---:|---|---|---|---|
| 1 | **Liger fused CE dW** | 根因 1：事件不平衡 | BF16 chunk 下 24/24 同向；4096 步后半程 64/64 个窗口仍超过自身随机基线 | **持久性 bias；并记录到参数/loss 分叉** |
| 2 | **Phi lm_head dX** | 根因 1：配对不平衡 | 自然配对有方向；打乱配对后消失；4096 步后半程仍超过自身随机基线；同协议随机舍入可消除 | **持久性 bias；小幅参数/loss 分叉** |
| 3 | Qwen lm_head dX | 根因 1 族；response 可抑制 | 32 步曾接近抵消，但 4096 步后半程仍超过自身随机基线 | **长程复核后仍是持久性 bias；AdamW 只削弱，不是完全抵消** |
| 4 | Phi norm `0543` | 未闭合 | 32 步单行名义过线 | 多重校正后未决 |
| 5 | Qwen v_proj rounding | 根因 1：舍入事件 | 固定状态下可见；4096 步直接方向未通过自己的基线 | **只有 loss 分叉的后果对照，不计为持久性 bias** |
| 6 | Qwen v_proj GEMM | 根因 1 候选 | 算子和 backward 均有证据 | 修复范围不一致，未决 |
| 7 | Mamba in_proj GEMM | 根因 1 候选 | 非 Transformer 也有非中心源 | 修复范围不一致，未决 |
| 8 | **Qwen saved-P** | **根因 2：响应不对称** | 严格 +δg/−δg 重放后 even response 非零；4096 步未通过持久门 | **有 loss 分叉的后果对照，不计为持久性 bias** |
| 9 | Qwen layer-23 attention | 根因 1：状态合同差异 | 修复 S_bwd 后方向消失 | 定位到语义区域；无统一 AdamW 标签 |
| 10 | **Qwen3-VL SiLU** | **根因 2：响应不对称** | 严格 antithetic 重放后 even response 非零；4096 步反馈方向在后半程持续 | **反馈维持型持久性 bias，并记录到 loss 分叉** |
| 11 | Gemma GELU/loss | 未确认根因 | 直接作用抵消 | 反馈对照，不升级 |
| 12 | DeepSeek dV BMM | 根因 1 候选 | gradient 中有方向 | 缺同一对照的完整链 |
| 13 | **Llama lm_head dX** | 已有族复现 | 4096 步 A=5.881，高于自身 null 1.030；记录到参数/loss 分叉 | **持久性 bias；同一实现族的新模型复现** |
| 14 | **Ministral lm_head dX** | 已有族复现 | 4096 步 A=5.050，高于自身 null 1.028；记录到参数/loss 分叉 | **持久性 bias；同一实现族的新模型复现** |

**怎么读这张表：**

#1、#2 解释**为什么正负事件没被公平组织**（根因 1）
#8、#10 解释**为什么严格正负输入仍得到不对称响应**（根因 2）
#3 说明 optimizer 也可以**把已有方向压回抵消**；但长程复核以 4096 步结果为准
#10、#11 说明“最终轨迹分离”不能反推 formation
未闭合的保留未决，不强行贴标签

按 4096 步严格口径，当前总审计表的 28 行中有 **6 个持久性 bias**：5 个直接方向案例（Liger、Phi/Qwen/Llama/Ministral 的 `lm_head dX` 家族）和 1 个反馈维持案例（Qwen3-VL SiLU）。其余记录要么是只有 loss 分叉的后果对照，要么仍未能安全重放。

【图 · Slide 7】这张表可以做成一张带颜色标注的大表。根因 1 用蓝色，根因 2 用橙色，未决用灰色。表旁标注三条读法。

---

# 第三部分：例子和证据

---

## Slide 8｜怎样测短程方向

在同一个训练状态下，只替换一个声明的实现边界，记录第 t 步真正写入参数的更新差异 u_t。

定义方向分数：

$$A_T = \frac{\|\sum_{t=1}^{T} u_t\|}{\sqrt{\sum_{t=1}^{T} \|u_t\|^2}}$$

**直觉**：醉汉走 32 步，每步随机，离原点大约 √32 步。清醒的人朝一个方向走 32 步，离原点 32 步。A 就是"实际走了多远 ÷ 醉汉该走多远"。

每个案例都和**自己的随机基线**比（把每步更新方向随机翻转），不用一个固定阈值。

$(A_{16},A_{32})$ 只叫**短程方向分数**，用于机制定位和实验排序，不叫 persistence。

【图 · Slide 8】醉汉 vs 清醒人的简笔画并排示意，下面标出 $A\approx1$（抵消）和 $A\gg1$（有方向）。公式可以放在角落小字。

---

## Slide 9｜根因 1 的例子：Liger 的正负事件从源头就不平衡

我们往 Liger 的输入里加了数学上完全无效的零行——loss 不变、有效梯度不变，但物理上的 chunk 分组变了。

| 累加精度 | 结果 |
|---|---|
| BF16 累加器 | 24 个状态里 **24 个同向**，sign-test p = 5.96×10⁻⁸ |
| FP32 累加器 | 13 比 11，方向不再一致 |

> 数学没变。**低精度累加和固定分块的组合**造成了正负事件的不平衡。

这是根因 1 最直接的例子。

---

## Slide 10｜根因 1 的例子：Phi 的问题是误差和反向状态的固定配对

Phi `lm_head dX` 的局部误差本身方向不强（A ≈ 2.07），但经过反向传播后，梯度方向分数跳到了 4.70。

**关键干预**：保留所有误差的大小和所有反向状态，**只打乱"哪个误差碰到哪个反向状态"**。结果方向消失了。

> 不是误差自己有方向，是**误差和反向状态的固定配对**制造了方向。这说明 formation 的关键不在单看局部误差大小。

---

## Slide 11｜根因 2 的例子：严格相反的误差也可能得到不相反的更新

为了直接检测响应是否保持对称，我们固定相同的权重、优化器状态、输入和随机种子，分别重放 +δg 和严格的 −δg。

如果响应对称，应有 F(+δg) = −F(−δg)。

我们拆出偶部 [F(+δg) + F(−δg)] / 2。对称响应下应该为零。

| 案例 | 观察 |
|---|---|
| **saved-P** | even response 非零，约为自然 response 的 0.677 倍 |
| **SiLU** | even response 非零，约为自然 response 的 0.693 倍；SGD 下为线性 null（如预期） |

> 输入严格正负对称，但优化器输出不是。这就是根因 2。

---

## Slide 12｜为什么优化器必须进入测试

优化器不是第三个根因，但**真正写进参数的是优化器的输出**。优化器是响应映射的一部分，正好处在根因 2 的检查位置。

Qwen 的短程结果最直观：

| 看哪一步 | 方向分数 |
|---|---:|
| 参数梯度 | 1.343 |
| AdamW 实际更新 | 0.961 |

如果只看梯度，会说"有方向"。看真正写进参数的更新，这个窗口里**方向没了**。

三个案例的对比：

| 案例 | 梯度方向分数 | AdamW 更新方向分数 | 结论 |
|---|---:|---:|---|
| Liger | 2.838 | **1.681** | 减弱但仍持续 |
| Phi | 4.683 | **1.030** | 大幅削弱，剩一点 |
| Qwen | 1.343 | **0.961** | 消掉了 |

> 优化器可以保留方向、削弱方向、也可以消掉方向。**所以检查必须做到实际参数更新，不能在梯度处停下。**

---

## Slide 13｜短程对照：方向分数不是在测误差能量

在同一处参数、同一把尺子下：

| 扰动 | 方向分数 | 一句话 |
|---|---:|---|
| 每步注入同等大小的随机干扰（5 种子） | 0.87–1.04 | "纯随机"在这把尺子下的样子 |
| 换数据顺序 | 0.007 | 同一组 batch 用完后几乎抵消 |
| **单个算子实现差异** | **4.701** | 远超随机 |

随机干扰**匹配了误差大小和参数位置**，但方向分数仍然接近 1。所以 4.701 反映的是误差被训练状态**怎样组织**，而不是误差能量本身。

---

## Slide 14｜误差大小预测不了是否形成 bias

32 个差异能到达参数的记录，误差大小和方向分数放一起看：

**Pearson r = 0.018，p = 0.921。基本不相关。**

直观反例：
- SiLU backward 的误差 L2 达到 21.37——**很大**——但短程方向接近抵消
- Phi 的误差**很小**，用 tolerance 测看起来最安全——但短程方向分数超过自身 null
- Liger 的误差大小和普通控制项**重叠**——却有明确的单侧 chunk 机制

> **误差大小可以衡量一个已确认问题有多严重，但不能告诉你 bias 存不存在。**

【图 · Slide 14】散点图：横轴局部误差 RMS，纵轴方向分数。三个根因 case 标出名字。看点：它们不是在右上角（大误差大方向），而是分散在各处。

---

## Slide 15｜因果证据：恢复正负公平，方向就消失

在 Phi 上做了因果实验：**完全相同的 32 个状态、相同参数、相同 AdamW 配置**，只把确定性 BF16 舍入换成随机舍入。

随机舍入不是消除误差，而是让每次舍入在两个相邻 BF16 值之间随机选，不再固定偏向一边。

| 实现 | 方向分数 | 自身随机上界 | 误差能量 / 原实现 |
|---|---:|---:|---:|
| 原始确定性 BF16 | **1.030** | 1.004 | 1.000 |
| 空操作对照 | **1.030** | 1.004 | 1.000 |
| 随机舍入 seed 0 | 1.000 | 1.002 | 1.009 |
| 随机舍入 seed 1 | 1.000 | 1.001 | 1.013 |
| 随机舍入 seed 2 | 1.000 | 1.002 | 1.067 |
| 随机舍入 seed 3 | 1.002 | 1.004 | 0.790 |

四个随机舍入全部回到抵消区间。**前三个的误差能量和原实现基本一样，两个还更高。**

> 方向消失不是因为误差变小——是因为**正负事件不再被固定规则偏向一边**。

这和 Verificarlo / Monte Carlo Arithmetic 的思路一脉相承（Parker 1997 提出，Denis et al. 2016 做成编译器工具）。但他们测的是方差和有效位数，面向推理；**我们测的是方向，面向训练。**

> 这是目前唯一一个在同一 AdamW 协议下完成"检测 → 空操作对照 → 因果干预 → 方向消失"的完整闭环。

【图 · Slide 15 · 重要】六个 arm 的方向分数条形图。natural 和 sham 高于 null 上界线，四个 SR 低于各自上界线。每个 bar 旁标误差能量比值。**这是全篇最强的因果图。**

---

# 第四部分：Oracle 与训练后果

---

## Slide 16｜统一 AdamW 下的短筛：只决定谁值得跑长程

所有行使用同一套 cold-start AdamW（moments 从零开始后正常演化，相同学习率和超参），每行与自己的 sign-flip null 比较。

| 案例 | A32 | 自身 null 上界 | 这一步能说明什么 |
|---|---:|---:|---|
| Liger fused CE | **1.720** | 1.116 | 升级到 4096 步；长程确认 |
| Phi lm_head dX | **1.029** | 1.004 | 升级到 4096 步；长程确认 |
| Qwen lm_head dX | 0.957 | 1.045 | 短窗口未升级，但长程复核后来确认 |
| Phi norm `0543` | 1.014 | 1.011 | 名义过线；多重校正后未决 |
| 其余 11 个结果盲选控制项 | 均无确认方向 | 各用自身上界 | 控制 |

排除未决项后，14 个已解决记录中 2 个正例、12 个负例，共 24 个正负排序对：

- **用方向分数排序**：24 对里只排错 **1 对**
- **用误差大小排序**：排错 **11 对**

> 这里的 16/32 步只负责排序和升级，不能单独叫持久性 bias。持久性标签来自后面的 4096 步后半程检验。

---

## Slide 17｜Oracle idea：先检查根因，再检查后果

目前不存在一个已验证的通用 Oracle，所以这里讲的是一个 idea，不是成品。

**Probe A — 配对公平性**：保留误差和训练状态的边际量，只打乱配对。自然配对有方向 + 打乱后消失 → 支持根因 1。（Phi 的干预是现有实例。）

**Probe B — 响应对称性**：固定 state/weights/moments，重放严格 +δ / −δ。F(+δ) + F(−δ) ≠ 0 → 支持根因 2。（saved-P 和 SiLU 是现有实例。）

**Probe C — 短程排序**：用 A16/A32 决定先深测谁。

```
Probe A / B（定位根因）
        ↓
A16 / A32 短程排序（决定优先级）
        ↓
配对长程实验（检查训练后果）
        ↓
bias only / bias + loss 分叉 / 未决
```

> Oracle 的创新不在造一个更复杂的分数，在于把问题分成两层：先定位抵消为何失效，再检查 bias 是否进入 loss。

---

## Slide 18｜长程后果对照：有 loss 分叉，但 bias 组件没有持续

案例地图回答了"哪里形成了 bias"。这一页回答另一件事：

> **有些配对实验确实出现了参数和 loss 分叉，但 4096 步后半程没有持续的 bias 组件。它们是后果对照，不是持久性 bias 案例。**

| 案例 | 早期形成证据 | 4096 步 bias 组件 | 配对后果 |
|---|---|---|---|
| Qwen seq64 v_proj | 条件源不对称 | A=0.993，未稳健保持 | 参数距离 0.220；后 512 步平均 loss gap +0.00410 |
| Qwen v_proj | 局部算术/配对 bias | A=0.981，未稳健保持 | 参数距离 0.302；后 512 步平均 loss gap −0.000615 |
| Qwen saved-P | 响应/状态合同不对称 | A=1.195，p=0.084，未过持久门 | 参数距离 0.123；后 512 步平均 loss gap −0.000150 |

这里的"loss 分叉"指：在声明的 4096 步受控配对实验中实际记录到 loss gap；它不等于证明两条完整训练轨迹会收敛到不同最优点，也不能替代 bias 本身的持久性证据。

Liger 和 lm_head dX 家族同时有 bias 和 loss 分叉，但它们已属于更强的 direct 子集，不在此重复。

---

## Slide 19｜反馈决定差异怎样长大，但不是根因

两条轨迹一旦分开，后续 weights、gradient、optimizer state 都不同。即使目标算子某一步几乎抵消，反馈也可能维持轨迹距离。

类比一个持续的小内存泄漏：每次只泄漏几字节，泄漏是真的。但最终 OOM 的大部分内存不是那行代码直接分配的——是系统状态的级联。**泄漏是触发器，级联是放大器。**

用 Liger 来说：**它的直接作用只占最终参数分离的 0.46%，但它是唯一持续的那部分。**不拆开看就会把 99.5% 的反馈也归给这个 kernel。

我们从 32 个可达记录中盲抽了 12 个跑完整轨迹——**11 个的权重最后都变了**。原因就是反馈放大，和算子是谁没关系。

> **"最后权重变了"不能反推目标算子持续朝同一边推。**根因和放大必须分开归因。

---

## Slide 20｜同一个实现差异，换参数位置结果就不同

同一个 Phi `lm_head dX` 的误差传到 12 个参数位置（1 个已知 anchor + 11 个结果盲选）：

| 参数位置 | 方向分数 |
|---|---:|
| final norm | **4.488** |
| layer 31 post-attention norm | **2.321** |
| layer 25 input norm | **2.226** |
| layer 8 post-attention norm | 1.192 |
| layer 16 post-attention norm | 1.033 |
| layer 12 input norm | 0.940 |
| layer 6 input norm | 0.578 |

> 测试对象不能只是"某个 kernel"。它得绑定：具体模型 + 具体调用 + 真实 backward + 参数位置 + 优化器。

（单参数受控实验，不是全参数训练。）

---

## Slide 21｜怎样用于真实开发流程

编译器 / kernel library / GPU backend 更新时：

```
找出实现变化的 forward + backward 路径
        ↓
过滤差异为零或到不了参数的
        ↓
构造最小修复和空操作对照
        ↓
第一层：bias formation
  检查事件/配对公平性 + 响应对称性
        ↓
用 A16/A32 排序后续实验优先级
        ↓
第二层：配对长程实验
  记录参数距离和 loss gap
        ↓
输出：bias only / bias + loss 分叉 / 反馈主导 / 未决
```

两个工程价值：
- **发现传统方法漏掉的**：误差很小，但正负组织不公平
- **避免错误拒绝高性能实现**：误差较大，但参数更新仍能抵消

> 交付的不是坏算子名单，而是**"先确认 bias，再检查是否进入 loss"的两层评估流程**。

---

## Slide 22｜当前边界

我们没有证明：
- 适用于所有模型和优化器的通用 Oracle
- 16 步没升级就等于安全
- 当前正例会导致完整训练崩溃
- 单参数位置等于全参数训练
- 能只看代码做静态预测
- 新实现类上的召回率已验证

**在 32 步尺度上后果还很小。**Phi 算子臂对 loss 的影响是百万分之一量级，比同一实验中的 BF16/FP32 对照还小。4096 步的 loss gap 也仍属于小效应。

> 我们提供的不是"训练已出事"的警报，是一种**在问题变严重之前就能看到它**的测量方法。

---

## Slide 23｜结论

**第一**：Tolerance 只回答误差有多大。我们的第一层回答：误差是否形成 bias。

**第二**：formation 归结为两类根因——正负事件或配对不平衡，以及正负响应不对称。低秩是几何形态，反馈是后续过程，都不是第三个根因。

**第三**：持久性 bias 的标签要求 bias 组件本身在 4096 步后半程仍持续；loss 分叉是它的后果证据。只有 loss 分叉而没有持续 bias 组件的记录，保留为后果对照，不计入案例数。

**第四**：工程流程是——确认 bias 怎样形成 → 短程决定先深测谁 → 配对长程检查是否进入 loss。

> 当两个实现都通过 tolerance 时，我们先问它们是否引入 bias，再问这个 bias 是否改变了训练的 loss。

---

## 参考文献

1. Qiu & Yao, *Why Low-Precision Transformer Training Fails*, ICLR 2026
2. Golden et al., *Is Flash Attention Stable?*, 2024
3. Denis et al., *Verificarlo: Checking FP Accuracy through Monte Carlo Arithmetic*, ARITH 2016
4. Parker, *Monte Carlo Arithmetic*, 1997
5. Karimireddy et al., *Error Feedback Fixes SignSGD*, ICML 2019
6. Ajalloeian & Stich, *On the Convergence of SGD with Biased Gradients*, 2020
