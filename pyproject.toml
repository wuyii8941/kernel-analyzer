[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kernel-analyzer"
version = "1.0.0"
description = "Compact evidence for all-op numerical bias analysis"
requires-python = ">=3.8"
